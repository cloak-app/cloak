{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "update",
  "description": "Capability for the update window",
  "windows": ["update"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
